{
  "name": "siyuan-plugin-claude-assistant",
  "version": "0.4.0",
  "type": "module",
  "description": "🤖 Claude AI Assistant plugin for SiYuan Note - Unified Chat & AI Text Editing with streaming responses",
  "repository": {
    "type": "git",
    "url": "https://github.com/Freedragonlancer/Siyuan-Note-ClaudePlugin.git"
  },
  "homepage": "https://github.com/Freedragonlancer/Siyuan-Note-ClaudePlugin#readme",
  "bugs": {
    "url": "https://github.com/Freedragonlancer/Siyuan-Note-ClaudePlugin/issues"
  },
  "keywords": [
    "siyuan-plugin",
    "claude-ai",
    "anthropic",
    "ai-assistant",
    "chatbot",
    "text-editing",
    "markdown",
    "typescript",
    "streaming",
    "note-taking"
  ],
  "author": "Freedragonlancer",
  "license": "MIT",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "deploy": "npm run build && npm run copy-plugin",
    "copy-plugin": "cp -v dist/index.js \"N:/Siyuan-Note/data/plugins/siyuan-plugin-claude-assistant/index.js\" && cp -v dist/index.css \"N:/Siyuan-Note/data/plugins/siyuan-plugin-claude-assistant/index.css\"",
    "make-link": "node scripts/make_dev_link.cjs",
    "make-link-win": "node scripts/make_dev_link.cjs",
    "bump-version": "node scripts/bump-version.cjs",
    "release": "npm run bump-version && git add package.json plugin.json && git commit -m \"chore: bump version to $(node -p \\\"require('./package.json').version\\\")\" && git tag v$(node -p \"require('./package.json').version\") && git push origin main --tags",
    "release:major": "npm run bump-version major && npm run release:commit-and-tag",
    "release:minor": "npm run bump-version minor && npm run release:commit-and-tag",
    "release:patch": "npm run bump-version patch && npm run release:commit-and-tag",
    "release:commit-and-tag": "git add package.json plugin.json && git commit -m \"chore: bump version to $(node -p \\\"require('./package.json').version\\\")\" && git tag v$(node -p \"require('./package.json').version\") && git push origin main --tags",
    "version:check": "node -p \"'package.json: ' + require('./package.json').version\" && node -p \"'plugin.json: ' + require('./plugin.json').version\""
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.1",
    "@tsconfig/svelte": "^5.0.2",
    "fast-glob": "^3.3.2",
    "minimist": "^1.2.8",
    "sass": "^1.69.5",
    "siyuan": "^1.1.5",
    "svelte": "^4.2.8",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "vite": "^5.0.8",
    "vite-plugin-static-copy": "^1.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.66.0",
    "@types/diff-match-patch": "^1.0.36",
    "diff-match-patch": "^1.0.5",
    "dompurify": "^3.3.0",
    "highlight.js": "^11.11.1",
    "marked": "^16.4.1"
  }
}
